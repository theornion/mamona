TEST ROUTINGU V4.5 — ZERO EDITS.

1. FAST_PARALLEL:
Uruchom w tym samym kroku dwa niezależne subagenty, bez czekania na pierwszy:
- mamona-diagnoser: znajdź tylko definicję wskazanej przez coordinator funkcji; read-only; zakończ po lokalizacji.
- mamona-executor: wykonaj wyłącznie `php -v`.

Podaj modele obu child sessions i potwierdź, czy Task calls zostały wysłane przed oczekiwaniem na wynik.

2. Nie uruchamiaj mamona-heavy-coder.
3. Niczego nie edytuj.
STOP.
